const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const pct=v=>Math.round(Math.max(0,Math.min(1,Number(v)||0))*100);
function addStyle(){if($('superUiStyle'))return;const s=document.createElement('style');s.id='superUiStyle';s.textContent=`.trace-card{grid-column:span 12}.trace-row{border-top:1px solid var(--line);padding:13px 0}.trace-row:first-child{border-top:0}.trace-source{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}.trace-chip{font-size:10px;padding:5px 8px;border:1px solid #28555b;border-radius:999px;background:#08191e}.trace-meta{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:9px}.trace-meta div{border:1px solid var(--line);border-radius:7px;padding:7px}.trace-meta small{display:block}.trace-label{font-size:9px;color:var(--muted);text-transform:uppercase;letter-spacing:.08em}.trace-warning{margin-top:8px;font-size:10px}.trace-warning.bad{color:#ff9b9b}.trace-warning.ok{color:#7de0bf}@media(max-width:900px){.trace-meta{grid-template-columns:1fr 1fr}}@media(max-width:600px){.trace-meta{grid-template-columns:1fr}}`;document.head.appendChild(s)}
function ensureTrace(){const v=$('view-evidence');if(!v||$('traceabilityPanel'))return;const p=document.createElement('article');p.id='traceabilityPanel';p.className='card trace-card';p.innerHTML='<h2>Evidence Chain · Source Traceability</h2><p class="sub">Every analytical claim is traceable to its source set. Independence is counted by origin group, not by the number of websites repeating the same material.</p><div id="traceabilityList"></div>';v.insertBefore(p,$('evidenceList')||null)}
function updateTrace(){const api=window.osintEnterprise;if(!api)return;const m=api.getIntelligenceMatrix?.();const list=$('traceabilityList');if(!list||!m)return;list.innerHTML=m.trace.map(t=>{const groups=[...new Set(t.sources.map(x=>x.independenceGroup||x.id))];return `<div class="trace-row"><div><strong>${esc(t.title||'Untitled evidence')}</strong><small>${esc(t.claim||'No claim recorded')}</small></div><div class="trace-meta"><div><span class="trace-label">Confidence</span><strong>${pct(t.confidence)}%</strong></div><div><span class="trace-label">Sources</span><strong>${t.sourceCount}</strong><small>${groups.length} independent group(s)</small></div><div><span class="trace-label">What it proves</span><small>${esc(t.whatProves||'Not specified')}</small></div><div><span class="trace-label">Does not prove</span><small>${esc(t.doesNotProve||'Not specified')}</small></div></div><div class="trace-source">${t.sources.map(x=>`<span class="trace-chip" title="${esc(x.provenance||x.publisher||x.type)}">${esc(x.name)} · ${pct(x.reliability)}%</span>`).join('')||'<span class="tag bad">NO SOURCE ATTACHED</span>'}</div>${groups.length<t.sourceCount?'<div class="trace-warning bad">⚠ Multiple source records resolve to the same independence group — do not count them as independent confirmation.</div>':''}${t.corroborating.length?`<small class="trace-warning ok">Corroborating sources: ${t.corroborating.length}</small>`:''}${t.contradicting.length?`<small class="trace-warning bad">Contradicting sources: ${t.contradicting.length}</small>`:''}</div>`}).join('')||'<div class="empty"><strong>No traceable evidence yet.</strong><small>Add evidence with at least one source.</small></div>'}
function bindTraceSearch(){const input=$('commandSearch');if(!input||input.dataset.traceBound)return;input.dataset.traceBound='1';input.addEventListener('input',e=>{const q=e.target.value.trim().toLowerCase();document.querySelectorAll('.data-card,.trace-row,.relationship-list .row').forEach(x=>x.style.display=!q||x.textContent.toLowerCase().includes(q)?'':'none')})}
function boot(){addStyle();ensureTrace();bindTraceSearch();let tries=0;const wait=setInterval(()=>{if(window.osintEnterprise){clearInterval(wait);updateTrace();setInterval(updateTrace,1000)}if(++tries>30)clearInterval(wait)},100)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,0),{once:true});else setTimeout(boot,0);
