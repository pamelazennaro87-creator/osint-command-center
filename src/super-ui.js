import { loadState } from './core/store.js';
import { buildIntelligenceMatrix } from './core/intelligence.js';
import { buildLiveSession, normalizeQuery } from './core/live-intelligence.js';
import { buildDAID2 } from './core/daid2.js';
const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const pct=v=>Math.round(Math.max(0,Math.min(1,Number(v)||0))*100);
function addStyle(){if($('superUiStyle'))return;const s=document.createElement('style');s.id='superUiStyle';s.textContent=`:root{--punk:#69d7d0}.daid-card{grid-column:span 12}.daid-console{border:1px solid #28555b;border-radius:13px;background:radial-gradient(650px 240px at 88% 0%,#16414a55,transparent 68%),#071217;padding:16px;box-shadow:0 18px 50px #0006;position:relative;overflow:hidden}.daid-console:before{content:'DAID 2.0 // COGNITIVE CONTROL LAYER';position:absolute;right:14px;top:12px;font:8px monospace;letter-spacing:.16em;color:#315c63}.daid-kicker{font:9px monospace;letter-spacing:.22em;color:var(--punk);text-shadow:0 0 12px #69d7d066}.daid-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.daid-head h2{margin:4px 0}.daid-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:14px}.daid-metric{border:1px solid var(--line);border-radius:9px;padding:10px;background:#09171c}.daid-metric strong{display:block;font-size:18px}.daid-metric small{display:block;margin-top:3px}.daid-flow{display:flex;gap:6px;flex-wrap:wrap;margin-top:12px}.daid-chip{font:9px monospace;letter-spacing:.08em;border:1px solid #28555b;border-radius:999px;padding:6px 8px;background:#08191e}.daid-chip.active{border-color:var(--punk);color:var(--punk);box-shadow:0 0 14px #69d7d01c}.daid-columns{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:12px}.daid-panel{border:1px solid var(--line);border-radius:9px;padding:11px}.daid-panel h3{font-size:11px;margin:0 0 7px}.daid-panel ul{margin:0;padding-left:17px}.daid-panel li{margin:5px 0;font-size:10px}.daid-next{margin-top:10px;border:1px solid #28555b;border-radius:9px;padding:11px;background:#08191e}.daid-next strong{color:var(--punk)}.trace-card,.live-card,.intake-card{grid-column:span 12}.trace-row{border-top:1px solid var(--line);padding:13px 0}.trace-row:first-child{border-top:0}.trace-source{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}.trace-chip{font-size:10px;padding:5px 8px;border:1px solid #28555b;border-radius:999px;background:#08191e}.trace-meta{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:9px}.trace-meta div{border:1px solid var(--line);border-radius:7px;padding:7px}.trace-meta small{display:block}.trace-label{font-size:9px;color:var(--muted);text-transform:uppercase;letter-spacing:.08em}.trace-warning{margin-top:8px;font-size:10px}.trace-warning.bad{color:#ff9b9b}.trace-warning.ok{color:#7de0bf}.live-console{border:1px solid #28555b;border-radius:13px;background:radial-gradient(600px 220px at 85% 0%,#16414a66,transparent 68%),repeating-linear-gradient(0deg,transparent 0 5px,#69d7d005 6px),#071217;padding:16px;box-shadow:0 0 0 1px #69d7d006,0 18px 50px #0006;position:relative;overflow:hidden}.live-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.live-kicker{font-size:9px;letter-spacing:.22em;color:var(--punk);text-shadow:0 0 12px #69d7d066}.live-head h2{margin:3px 0 4px;font-size:18px}.live-query{display:flex;gap:8px;margin-top:14px}.live-query input{flex:1;min-width:0;background:#050d11;border:1px solid var(--line);color:var(--text);padding:11px 12px;border-radius:8px;outline:none}.live-query input:focus{border-color:var(--punk)}.live-results{display:grid;grid-template-columns:repeat(2,1fr);gap:9px;margin-top:12px}.live-result{border:1px solid var(--line);border-radius:10px;padding:12px;background:#09171c;cursor:pointer;transition:.16s;position:relative}.live-result:hover,.live-result:focus{border-color:var(--punk);transform:translateY(-2px);outline:none}.live-result strong{display:block;font-size:12px}.live-result small{display:block;margin-top:4px}.live-badges{display:flex;gap:6px;flex-wrap:wrap;margin-top:9px}.live-badge{font-size:9px;border:1px solid var(--line);border-radius:999px;padding:4px 7px}.live-badge.raw{color:var(--warn)}.live-badge.ind{color:var(--ok)}.live-banner{margin-top:12px;padding:10px;border:1px solid #604d29;border-radius:8px;color:#e6bd6a;background:#151209}.live-detail{margin-top:12px;border-top:1px solid var(--line);padding-top:12px;display:none}.live-detail.open{display:block}.live-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.live-actions button{border:1px solid var(--line);background:#0d2027;color:var(--text);padding:8px 10px;border-radius:7px;cursor:pointer}.live-actions button:hover{border-color:var(--punk)}.live-actions button.primary{background:var(--punk);color:#041012;font-weight:800}.intake-drop{border:1px dashed #37606a;border-radius:14px;padding:22px;text-align:center;background:#07151a}.intake-drop.drag{border-color:var(--punk);background:#0a2025}.intake-drop strong{display:block;font-size:14px}.intake-drop small{display:block;margin:5px 0 13px}.intake-drop input{display:none}.intake-files{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px}.intake-file{border:1px solid var(--line);border-radius:9px;padding:10px;text-align:left;background:#08161b}.intake-file strong{display:block;font-size:11px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.intake-file small{display:block;margin-top:3px}.intake-note{margin-top:9px;font-size:9px;color:var(--muted)}.punk-hud{position:relative;border:1px solid #28555b!important;background:radial-gradient(450px 180px at 90% 0%,#69d7d012,transparent 70%)!important}.entity-inspector{position:fixed;right:18px;bottom:18px;width:min(390px,calc(100vw - 36px));z-index:50;border:1px solid var(--punk);background:#061014f5;border-radius:12px;padding:15px;box-shadow:0 20px 60px #0009;display:none}.entity-inspector.open{display:block}.entity-inspector .x{float:right;background:none;border:0;color:var(--muted);font-size:18px;cursor:pointer}.entity-inspector .ey{font:9px monospace;letter-spacing:.16em;color:var(--punk)}.entity-inspector h3{margin:4px 0}.ins-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:10px}.ins-grid div{border:1px solid var(--line);border-radius:7px;padding:7px}.ins-grid small{display:block}.graph-canvas .entity-node{cursor:pointer}.graph-canvas .entity-node:hover .node,.graph-canvas .entity-node:focus .node{filter:drop-shadow(0 0 8px #69d7d088);stroke:var(--punk);stroke-width:2}.graph-canvas .entity-node:focus{outline:none}@media(max-width:900px){.daid-grid{grid-template-columns:1fr 1fr}.daid-columns{grid-template-columns:1fr}.trace-meta{grid-template-columns:1fr 1fr}.live-results,.intake-files{grid-template-columns:1fr 1fr}}@media(max-width:600px){.daid-grid,.trace-meta,.live-results,.intake-files{grid-template-columns:1fr}.live-query{display:block}.live-query button{margin-top:7px;width:100%}.entity-inspector{right:10px;bottom:10px;width:calc(100vw - 20px)}}`;document.head.appendChild(s)}
function ensureDAID(){const v=$('view-command');if(!v||$('daidPanel'))return;const p=document.createElement('article');p.id='daidPanel';p.className='card daid-card';p.innerHTML='<div class="daid-console"><div class="daid-head"><div><div class="daid-kicker">◈ DAID 2.0</div><h2>Cognitive Control Layer</h2><small>Dictionary AI Dynamic · challenge the reasoning path before it becomes a conclusion.</small></div><span id="daidMode" class="tag warn">OBSERVE</span></div><div id="daidMetrics" class="daid-grid"></div><div id="daidFlow" class="daid-flow"></div><div class="daid-columns"><section class="daid-panel"><h3>FAILURE SIGNATURES</h3><ul id="daidFailures"></ul></section><section class="daid-panel"><h3>REPAIR MODE</h3><ul id="daidRepairs"></ul></section></div><div id="daidNext" class="daid-next"></div></div>';v.prepend(p)}
function updateDAID(){const s=loadState(),m=buildIntelligenceMatrix(s),d=buildDAID2(s,m);if($('daidMode')){$('daidMode').textContent=d.mode; $('daidMode').className=`tag ${d.mode==='DECIDE'?'ok':d.mode==='REPAIR'?'bad':'warn'}`}if($('daidMetrics'))$('daidMetrics').innerHTML=[['SIGNAL DEBT',d.signalDebt+'%'],['PATTERN OVERSHOOT',d.patternOvershootRisk+'%'],['RESPONSE DRIFT',d.artificialResponseDrift+'%'],['COGNITIVE SYNC',d.cognitiveSync+'%']].map(x=>`<div class="daid-metric"><strong>${esc(x[1])}</strong><small>${x[0]}</small></div>`).join('');if($('daidFlow'))$('daidFlow').innerHTML=['INTENT ENVELOPE','OBSERVE','CHALLENGE','TEST','REPAIR','DECIDE'].map(x=>`<span class="daid-chip ${x===d.mode||x==='INTENT ENVELOPE'?'active':''}">${x}</span>`).join('');if($('daidFailures'))$('daidFailures').innerHTML=d.failureSignatures.map(x=>`<li>${esc(x.replaceAll('_',' '))}</li>`).join('')||'<li>No active failure signature.</li>';if($('daidRepairs'))$('daidRepairs').innerHTML=d.repairModes.slice(0,5).map(x=>`<li><strong>${esc(x.action.replaceAll('_',' '))}</strong> — ${esc(x.reason)}</li>`).join('')||'<li>Maintain adversarial review.</li>';if($('daidNext'))$('daidNext').innerHTML=`<strong>NEXT BEST ACTION · ${esc(d.nextBestAction.action.replaceAll('_',' '))}</strong><br><small>${esc(d.nextBestAction.reason)}</small><br><small>Uncertainty: ${esc(d.uncertainty)} · Intent bounded: ${d.intentEnvelope.bounded?'YES':'NO'}</small>`}
function ensureTrace(){const v=$('view-evidence');if(!v||$('traceabilityPanel'))return;const p=document.createElement('article');p.id='traceabilityPanel';p.className='card trace-card';p.innerHTML='<h2>Evidence Chain · Source Traceability</h2><p class="sub">Every analytical claim is traceable to its source set. Independence is counted by origin group.</p><div id="traceabilityList"></div>';v.insertBefore(p,$('evidenceList')||null)}
function updateTrace(){const list=$('traceabilityList');if(!list)return;const m=buildIntelligenceMatrix(loadState());list.innerHTML=(m.trace||[]).map(t=>{const groups=[...new Set(t.sources.map(x=>x.independenceGroup||x.id))];return `<div class="trace-row"><div><strong>${esc(t.title||'Untitled evidence')}</strong><small>${esc(t.claim||'No claim recorded')}</small></div><div class="trace-meta"><div><span class="trace-label">Confidence</span><strong>${pct(t.confidence)}%</strong></div><div><span class="trace-label">Sources</span><strong>${t.sourceCount}</strong><small>${groups.length} independent group(s)</small></div><div><span class="trace-label">What it proves</span><small>${esc(t.whatProves||'Not specified')}</small></div><div><span class="trace-label">Does not prove</span><small>${esc(t.doesNotProve||'Not specified')}</small></div></div><div class="trace-source">${t.sources.map(x=>`<span class="trace-chip">${esc(x.name)} · ${pct(x.reliability)}%</span>`).join('')||'<span class="tag bad">NO SOURCE ATTACHED</span>'}</div></div>`}).join('')||'<div class="empty"><strong>No traceable evidence yet.</strong><small>Add evidence with at least one source.</small></div>'}
function ensureLive(){const v=$('view-evidence');if(!v||$('liveIntelligencePanel'))return;const p=document.createElement('article');p.id='liveIntelligencePanel';p.className='card live-card';p.innerHTML='<div class="live-console"><div class="live-head"><div><div class="live-kicker">◉ LIVE INTELLIGENCE</div><h2>Public-source investigation console</h2><small>Discovery remains RAW until reviewed.</small></div><span class="tag warn">RAW BY DEFAULT</span></div><form id="liveSearchForm" class="live-query"><input id="liveQuery" maxlength="240" autocomplete="off" placeholder="Search a person, organisation, vessel, event…" aria-label="Live intelligence query"><button class="button primary" type="submit">Search</button></form><div class="live-banner">ONLINE DATA ≠ VERIFIED EVIDENCE · External providers may see query terms.</div><div id="liveResults" class="live-results"></div><div id="liveDetail" class="live-detail"></div></div>';v.prepend(p);$('liveSearchForm').addEventListener('submit',e=>{e.preventDefault();runLiveSearch()})}
function runLiveSearch(){const q=normalizeQuery($('liveQuery')?.value),out=$('liveResults');if(!out)return;if(!q){out.innerHTML='<div class="empty">Enter a focused research query.</div>';return}const session=buildLiveSession(q,[{title:`Search plan · ${q}`,publisher:'Search adapter ready',sourceType:'search-plan',independenceGroup:'search-plan'},{title:'Primary-source route',publisher:'Institutional / registry lookup',sourceType:'primary-source',independenceGroup:'primary-source'},{title:'Contradiction route',publisher:'Independent-source challenge',sourceType:'challenge',independenceGroup:'challenge'}]);window.__liveSession=session;out.innerHTML=session.results.map((r,i)=>`<article class="live-result" tabindex="0" data-live-index="${i}"><strong>${esc(r.title)}</strong><small>${esc(r.publisher)}</small><div class="live-badges"><span class="live-badge raw">${r.assessment}</span><span class="live-badge">${esc(r.sourceType)}</span><span class="live-badge ind">origin ${esc(r.independenceGroup)}</span></div></article>`).join('');out.querySelectorAll('[data-live-index]').forEach(el=>{el.addEventListener('click',()=>showLiveDetail(Number(el.dataset.liveIndex)));el.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();showLiveDetail(Number(el.dataset.liveIndex))}})})}
function showLiveDetail(i){const s=window.__liveSession,r=s?.results?.[i],d=$('liveDetail');if(!r||!d)return;d.className='live-detail open';d.innerHTML=`<strong>${esc(r.title)}</strong><small>${esc(r.publisher)} · ${esc(r.accessedAt)}</small><div class="live-actions"><button class="primary" data-live-action="promote">Review → Evidence</button><button data-live-action="primary">Find primary source</button><button data-live-action="challenge">Search contradiction</button><button data-live-action="temporal">Check temporal snapshot</button></div><small class="intake-note">Promotion remains UNKNOWN until human review.</small>`;d.querySelector('[data-live-action="promote"]').addEventListener('click',()=>{d.insertAdjacentHTML('beforeend','<div class="live-banner">Candidate staged for human review.</div>')});d.querySelectorAll('[data-live-action]:not([data-live-action="promote"])').forEach(b=>b.addEventListener('click',()=>{b.textContent='Queued · provider adapter required';b.disabled=true}))}
function ensureIntake(){const v=$('view-evidence');if(!v||$('evidenceIntake'))return;const p=document.createElement('article');p.id='evidenceIntake';p.className='card intake-card';p.innerHTML='<h2>Evidence Intake · drop, inspect, preserve</h2><p class="sub">Files remain local; this interface does not upload bytes.</p><div id="intakeDrop" class="intake-drop"><strong>Drop evidence here</strong><small>or choose screenshots / PDF / text / documents / images</small><label class="button primary" for="evidenceFiles">＋ Choose evidence</label><input id="evidenceFiles" type="file" multiple accept="image/*,application/pdf,text/plain,.csv,.json,.doc,.docx,.xlsx"></div><div id="intakeFiles" class="intake-files"></div><div class="intake-note">15 MB/file intake limit. Files are staged only; analytical metadata remains separate.</div></article>';v.insertBefore(p,$('traceabilityPanel')||null);const input=$('evidenceFiles'),drop=$('intakeDrop');input.addEventListener('change',()=>stageFiles(input.files));['dragenter','dragover'].forEach(e=>drop.addEventListener(e,ev=>{ev.preventDefault();drop.classList.add('drag')}));['dragleave','drop'].forEach(e=>drop.addEventListener(e,ev=>{ev.preventDefault();drop.classList.remove('drag')}));drop.addEventListener('drop',ev=>stageFiles(ev.dataTransfer.files))}
function stageFiles(files){const out=$('intakeFiles');if(!out)return;const arr=[...files].filter(f=>f.size<=15*1024*1024).slice(0,12);out.innerHTML=arr.map(f=>`<div class="intake-file"><strong title="${esc(f.name)}">${esc(f.name)}</strong><small>${esc(f.type||'unknown')} · ${(f.size/1024/1024).toFixed(2)} MB</small><span class="tag ok">READY FOR REVIEW</span></div>`).join('')||'<div class="empty">No supported files selected, or files exceed the 15 MB intake limit.</div>'}
function inspectEntity(id){const s=loadState(),e=s.entities.find(x=>x.id===id);if(!e)return;let box=$('entityInspector');if(!box){box=document.createElement('aside');box.id='entityInspector';box.className='entity-inspector';document.body.appendChild(box)}const rel=(s.relationships||[]).filter(r=>r.fromEntityId===id||r.toEntityId===id),ev=s.evidence.filter(x=>(rel||[]).some(r=>(r.evidenceIds||[]).includes(x.id)));box.innerHTML=`<button class="x" aria-label="Close">×</button><div class="ey">ENTITY INSPECTOR</div><h3>${esc(e.name)}</h3><small>${esc(e.type)} · ${esc(e.id)}</small><div class="ins-grid"><div><span class="trace-label">Relationships</span><strong>${rel.length}</strong></div><div><span class="trace-label">Linked evidence</span><strong>${ev.length}</strong></div></div><div class="trace-source">${rel.map(r=>{const other=s.entities.find(x=>x.id===(r.fromEntityId===id?r.toEntityId:r.fromEntityId));return `<span class="trace-chip">${esc(r.type)} → ${esc(other?.name||'?')}</span>`}).join('')||'<span class="tag warn">NO LINKS</span>'}</div>`;box.classList.add('open');box.querySelector('.x').onclick=()=>box.classList.remove('open')}
function bindGraph(){document.querySelectorAll('.entity-node').forEach(n=>{if(n.dataset.bound)return;n.dataset.bound='1';const act=()=>inspectEntity(n.dataset.entityId);n.addEventListener('click',act);n.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();act()}})})}
function polish(){document.querySelectorAll('.card').forEach(c=>c.classList.add('punk-hud'))}
function init(){addStyle();ensureDAID();ensureTrace();ensureLive();ensureIntake();updateDAID();updateTrace();polish();bindGraph();setInterval(()=>{ensureDAID();ensureTrace();ensureLive();ensureIntake();updateDAID();updateTrace();polish();bindGraph()},1000);document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='l'){$('liveQuery')?.focus();e.preventDefault()}})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
