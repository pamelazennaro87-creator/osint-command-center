import { loadState } from './core/store.js';
import { buildIntelligenceMatrix } from './core/intelligence.js';
import { buildLiveSession, normalizeQuery } from './core/live-intelligence.js';
const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const pct=v=>Math.round(Math.max(0,Math.min(1,Number(v)||0))*100);
function addStyle(){if($('superUiStyle'))return;const s=document.createElement('style');s.id='superUiStyle';s.textContent=`.trace-card,.live-card,.intake-card{grid-column:span 12}.trace-row{border-top:1px solid var(--line);padding:13px 0}.trace-row:first-child{border-top:0}.trace-source{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}.trace-chip{font-size:10px;padding:5px 8px;border:1px solid #28555b;border-radius:999px;background:#08191e}.trace-meta{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:9px}.trace-meta div{border:1px solid var(--line);border-radius:7px;padding:7px}.trace-meta small{display:block}.trace-label{font-size:9px;color:var(--muted);text-transform:uppercase;letter-spacing:.08em}.trace-warning{margin-top:8px;font-size:10px}.trace-warning.bad{color:#ff9b9b}.trace-warning.ok{color:#7de0bf}.live-console{border:1px solid #28555b;border-radius:13px;background:radial-gradient(500px 180px at 80% 0%,#16414a55,transparent 70%),#071217;padding:16px}.live-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.live-kicker{font-size:9px;letter-spacing:.18em;color:var(--a)}.live-head h2{margin:3px 0 4px;font-size:18px}.live-query{display:flex;gap:8px;margin-top:14px}.live-query input{flex:1;min-width:0;background:#050d11;border:1px solid var(--line);color:var(--text);padding:11px 12px;border-radius:8px;outline:none}.live-query input:focus{border-color:var(--a);box-shadow:0 0 0 3px #69d7d014}.live-results{display:grid;grid-template-columns:repeat(2,1fr);gap:9px;margin-top:12px}.live-result{border:1px solid var(--line);border-radius:10px;padding:12px;background:#09171c;cursor:pointer;transition:.16s}.live-result:hover,.live-result:focus{border-color:#37606a;transform:translateY(-1px);outline:none}.live-result strong{display:block;font-size:12px}.live-result small{display:block;margin-top:4px}.live-badges{display:flex;gap:6px;flex-wrap:wrap;margin-top:9px}.live-badge{font-size:9px;border:1px solid var(--line);border-radius:999px;padding:4px 7px}.live-badge.raw{color:var(--warn)}.live-badge.ind{color:var(--ok)}.live-banner{margin-top:12px;padding:10px;border:1px solid #604d29;border-radius:8px;color:#e6bd6a;background:#151209}.live-detail{margin-top:12px;border-top:1px solid var(--line);padding-top:12px;display:none}.live-detail.open{display:block}.live-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.live-actions button{border:1px solid var(--line);background:#0d2027;color:var(--text);padding:8px 10px;border-radius:7px;cursor:pointer}.live-actions button.primary{background:var(--a);color:#041012;font-weight:800}.intake-drop{border:1px dashed #37606a;border-radius:14px;padding:22px;text-align:center;background:radial-gradient(400px 150px at 50% 0%,#69d7d00b,transparent 70%);transition:.18s}.intake-drop.drag{border-color:var(--a);background:#0a2025}.intake-drop strong{display:block;font-size:14px}.intake-drop small{display:block;margin:5px 0 13px}.intake-drop input{display:none}.intake-files{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px}.intake-file{border:1px solid var(--line);border-radius:9px;padding:10px;text-align:left;background:#08161b}.intake-file strong{display:block;font-size:11px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.intake-file small{display:block;margin-top:3px}.intake-note{margin-top:9px;font-size:9px;color:var(--muted)}@media(max-width:900px){.trace-meta{grid-template-columns:1fr 1fr}.live-results,.intake-files{grid-template-columns:1fr 1fr}}@media(max-width:600px){.trace-meta,.live-results,.intake-files{grid-template-columns:1fr}.live-query{display:block}.live-query button{margin-top:7px;width:100%}}`;document.head.appendChild(s)}
function ensureTrace(){const v=$('view-evidence');if(!v||$('traceabilityPanel'))return;const p=document.createElement('article');p.id='traceabilityPanel';p.className='card trace-card';p.innerHTML='<h2>Evidence Chain · Source Traceability</h2><p class="sub">Every analytical claim is traceable to its source set. Independence is counted by origin group, not by the number of websites repeating the same material.</p><div id="traceabilityList"></div>';v.insertBefore(p,$('evidenceList')||null)}
function updateTrace(){const list=$('traceabilityList');if(!list)return;const m=buildIntelligenceMatrix(loadState());list.innerHTML=m.trace.map(t=>{const groups=[...new Set(t.sources.map(x=>x.independenceGroup||x.id))];return `<div class="trace-row"><div><strong>${esc(t.title||'Untitled evidence')}</strong><small>${esc(t.claim||'No claim recorded')}</small></div><div class="trace-meta"><div><span class="trace-label">Confidence</span><strong>${pct(t.confidence)}%</strong></div><div><span class="trace-label">Sources</span><strong>${t.sourceCount}</strong><small>${groups.length} independent group(s)</small></div><div><span class="trace-label">What it proves</span><small>${esc(t.whatProves||'Not specified')}</small></div><div><span class="trace-label">Does not prove</span><small>${esc(t.doesNotProve||'Not specified')}</small></div></div><div class="trace-source">${t.sources.map(x=>`<span class="trace-chip" title="${esc(x.provenance||x.publisher||x.type)}">${esc(x.name)} · ${pct(x.reliability)}%</span>`).join('')||'<span class="tag bad">NO SOURCE ATTACHED</span>'}</div>${groups.length<t.sourceCount?'<div class="trace-warning bad">⚠ Multiple source records resolve to the same independence group — do not count them as independent confirmation.</div>':''}${t.corroborating.length?`<small class="trace-warning ok">Corroborating sources: ${t.corroborating.length}</small>`:''}${t.contradicting.length?`<small class="trace-warning bad">Contradicting sources: ${t.contradicting.length}</small>`:''}</div>`}).join('')||'<div class="empty"><strong>No traceable evidence yet.</strong><small>Add evidence with at least one source.</small></div>'}
function ensureLive(){const v=$('view-evidence');if(!v||$('liveIntelligencePanel'))return;const p=document.createElement('article');p.id='liveIntelligencePanel';p.className='card live-card';p.innerHTML='<div class="live-console"><div class="live-head"><div><div class="live-kicker">◉ LIVE INTELLIGENCE</div><h2>Public-source investigation console</h2><small>Interactive discovery with provenance separation. Search results are never promoted to verified evidence automatically.</small></div><span class="tag warn">RAW BY DEFAULT</span></div><form id="liveSearchForm" class="live-query"><input id="liveQuery" maxlength="240" autocomplete="off" placeholder="Search a person, organisation, vessel, event…" aria-label="Live intelligence query"><button class="button primary" type="submit">Search</button></form><div class="live-banner">ONLINE DATA ≠ VERIFIED EVIDENCE · Querying external services can reveal search terms to those providers. Use redacted queries or a private backend when operational privacy matters.</div><div id="liveResults" class="live-results"></div><div id="liveDetail" class="live-detail"></div></div>';v.prepend(p);$('liveSearchForm').addEventListener('submit',e=>{e.preventDefault();runLiveSearch()})}
function runLiveSearch(){const q=normalizeQuery($('liveQuery')?.value);const out=$('liveResults');if(!out)return;if(!q){out.innerHTML='<div class="empty">Enter a focused research query.</div>';return}const seed=[{title:`Search plan · ${q}`,publisher:'Search adapter ready',url:'',sourceType:'search-plan',independenceGroup:'search-plan'},{title:'Primary-source route',publisher:'Institutional / registry lookup',url:'',sourceType:'primary-source',independenceGroup:'primary-source'},{title:'Contradiction route',publisher:'Independent-source challenge',url:'',sourceType:'challenge',independenceGroup:'challenge'}];const session=buildLiveSession(q,seed);window.__liveSession=session;out.innerHTML=session.results.map((r,i)=>`<article class="live-result" tabindex="0" data-live-index="${i}"><strong>${esc(r.title)}</strong><small>${esc(r.publisher)}</small><div class="live-badges"><span class="live-badge raw">${r.assessment}</span><span class="live-badge">${esc(r.sourceType)}</span><span class="live-badge ind">origin ${esc(r.independenceGroup)}</span></div></article>`).join('');out.querySelectorAll('[data-live-index]').forEach(el=>el.addEventListener('click',()=>showLiveDetail(Number(el.dataset.liveIndex))));}
function showLiveDetail(i){const s=window.__liveSession,r=s?.results?.[i],d=$('liveDetail');if(!r||!d)return;d.className='live-detail open';d.innerHTML=`<strong>${esc(r.title)}</strong><small>${esc(r.publisher)} · ${esc(r.domain||'no URL yet')} · captured ${esc(r.accessedAt)}</small><div class="live-actions"><button class="primary" data-live-action="promote">Review → Evidence</button><button data-live-action="primary">Find primary source</button><button data-live-action="challenge">Search contradiction</button><button data-live-action="temporal">Check temporal snapshot</button></div><small class="intake-note">Promotion creates an UNKNOWN evidence candidate. Human verification remains mandatory.</small>`;d.querySelector('[data-live-action="promote"]').addEventListener('click',()=>{d.insertAdjacentHTML('beforeend','<div class="live-banner">Candidate staged. Add a source-specific claim and review it before marking verified.</div>')});d.querySelectorAll('[data-live-action]:not([data-live-action="promote"])').forEach(b=>b.addEventListener('click',()=>{b.textContent='Queued · requires provider adapter';b.disabled=true}))}
function ensureIntake(){const v=$('view-evidence');if(!v||$('evidenceIntake'))return;const p=document.createElement('article');p.id='evidenceIntake';p.className='card intake-card';p.innerHTML='<h2>Evidence Intake · drop, inspect, preserve</h2><p class="sub">Screenshots, PDFs and other supporting files can enter through one controlled intake point. Files stay local to the browser; only metadata belongs in the analytical state.</p><div id="intakeDrop" class="intake-drop"><strong>Drop evidence here</strong><small>or choose files · screenshots / PDF / text / documents / images</small><label class="button primary" for="evidenceFiles">＋ Choose evidence</label><input id="evidenceFiles" type="file" multiple accept="image/*,application/pdf,text/plain,.csv,.json,.doc,.docx,.xlsx"></div><div id="intakeFiles" class="intake-files"></div><div class="intake-note">Privacy boundary: file bytes are not uploaded by this interface. Large files are not copied into localStorage.</div></article>';v.insertBefore(p,$('traceabilityPanel')||null);const input=$('evidenceFiles'),drop=$('intakeDrop');input.addEventListener('change',()=>stageFiles(input.files));['dragenter','dragover'].forEach(e=>drop.addEventListener(e,ev=>{ev.preventDefault();drop.classList.add('drag')}));['dragleave','drop'].forEach(e=>drop.addEventListener(e,ev=>{ev.preventDefault();drop.classList.remove('drag')}));drop.addEventListener('drop',ev=>stageFiles(ev.dataTransfer.files))}
function stageFiles(files){const out=$('intakeFiles');if(!out)return;const arr=[...files].filter(f=>f.size<=15*1024*1024).slice(0,12);out.innerHTML=arr.map(f=>`<div class="intake-file"><strong title="${esc(f.name)}">${esc(f.name)}</strong><small>${esc(f.type||'unknown')} · ${(f.size/1024/1024).toFixed(2)} MB</small><span class="tag ok">READY FOR REVIEW</span></div>`).join('')||'<div class="empty">No supported files selected, or files exceed the 15 MB intake limit.</div>'}
function bindTraceSearch(){const input=$('commandSearch');if(!input||input.dataset.traceBound)return;input.dataset.traceBound='1';input.addEventListener('input',e=>{const q=e.target.value.trim().toLowerCase();document.querySelectorAll('.data-card,.trace-row,.relationship-list .row').forEach(x=>x.style.display=!q||x.textContent.toLowerCase().includes(q)?'':'none')})}
function boot(){addStyle();ensureTrace();ensureLive();ensureIntake();bindTraceSearch();updateTrace();setInterval(updateTrace,1000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,0),{once:true});else setTimeout(boot,0);
